spring:
  datasource:
    url: jdbc:mysql://localhost:3306/flowquest
    username: user
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: update
    show-sql: true
  flyway:
    enabled: false
